{
  "name": "@archai/agent-webui-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and utilities for agent-webui (AG-UI / OpenCode / MCP-UI).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
